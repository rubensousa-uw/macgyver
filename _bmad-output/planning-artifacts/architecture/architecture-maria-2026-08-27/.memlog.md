---
scope: Meta Adventurer camera milestone
purpose: Build substrate for epics and implementation
altitude: feature
updated: 2026-08-27T23:09
---

- (direction) Fast path selected; draft the full spine with [ASSUMPTION] tags for inferred decisions.
- (decision) The architecture spine is the only deliverable; no additional audience-specific rendering is required.
- (constraint) The canonical SPEC and all named companions bind this feature-level architecture.
- (constraint) The VisionClaw source is not present in the maria workspace, so brownfield claims must be verified against the current upstream repository or marked as assumptions pending local import.
- (version) VisionClaw upstream main inspected at commit fbc72a2; its Android sample pins DAT 0.4.0, AGP 8.6.0, Kotlin 2.1.20, Compose BOM 2024.04.01, compileSdk 35, targetSdk 34, and minSdk 31.
- (version) Meta official Android DAT repository inspected at release commit 81dfb51; latest stable 0.9.x is 0.9.0 released 2026-08-03.
- (decision) Bind all milestone units to ports-and-adapters with unidirectional state flow: UI and future layers depend on WearableAdapter domain contracts, and only the Meta adapter imports DAT. This prevents DAT types and lifecycle assumptions leaking into diagnostics or future providers.
- (decision) A process-scoped WearableRuntime is the sole owner of the active device identity, DeviceSession, Camera, Stream, transition reducer, and session telemetry. This prevents multiple ViewModels or services from racing to create or tear down hardware resources.
- (decision) All commands and SDK callbacks enter one serialized event reducer; it alone mutates the canonical WearableSnapshot and emits transition events. This prevents observers from constructing incompatible state from parallel flows.
- (decision) FIRST_FRAME_RECEIVED is emitted only for a usable non-codec frame payload; SDK STARTED or STREAMING states map to STREAM_WAITING until that event. This prevents false vision readiness.
- (decision) Copy frame bytes out of the DAT callback before publication and expose only app-owned frame envelopes on a bounded latest-frame channel; count every valid received frame before conflation. This prevents DAT buffer lifetime bugs and slow consumers blocking the SDK collector.
- (decision) Use the DAT 0.9.0 lifecycle Wearables.createSession then DeviceSession.addCamera then Camera.stream.start; teardown is idempotent and ordered through Camera.stop or removeCamera before DeviceSession.stop. This prevents mixing 0.4 and 0.9 lifecycle models or leaving attached capabilities.
- (decision) Recovery is state-driven and owned by the runtime: terminal/disconnect events cancel collectors and clean up exactly once; a new session may be created only when an eligible connected device reappears and the user still requests streaming. This prevents duplicate retry loops and resurrection after explicit stop.
- (decision) Diagnostics and structured logs are projections of the same canonical snapshot and transition stream; clocks for latencies and FPS are monotonic and counters are session-scoped. This prevents UI, telemetry, and logs disagreeing.
- (decision) Failures are classified at DEVICE, DAT, CAMERA, NETWORK, or AUTH boundaries and retain the typed DAT cause internally; logs redact secrets and never capture raw private audio. This prevents generic AI errors and privacy leakage.
- (decision) Verification is layered: pure reducer and telemetry tests, DAT 0.9 MockDeviceKit instrumentation, then the physical Adventurer acceptance matrix. Only physical gates authorize a hardware-working claim or tag.
- (decision) Establish and record the unchanged VisionClaw baseline before migration, pin only DAT 0.9.0 initially, and do not opportunistically upgrade AGP, Kotlin, Compose, or SDK levels unless compilation forces a separately documented change. This prevents baseline and migration failures from being conflated.
- (assumption) The process-scoped runtime can be introduced through a small application container without adding a dependency-injection framework or moving unrelated VisionClaw features.
- (assumption) The single-device milestone selects one eligible connected device and locks its identifier for the lifetime of a session; multi-device switching is deferred.
- (question) The architecture must be ratified against the exact locally imported VisionClaw commit before implementation because the maria workspace currently contains no upstream source checkout.
- (event) Source and current-version investigation complete; architecture decisions ready for distillation.
- (decision) Teardown requires the supported DAT 0.9 equivalents of Camera.stop, then DeviceSession.removeCamera, then DeviceSession.stop; all steps remain idempotent.
- (decision) Preserve compatible VisionClaw phone fallback, throttling, audio, Gemini, OpenClaw, reconnect, WebRTC, settings, and session-state behavior; inspect each integration surface before editing and do not rewrite unrelated paths.
- (constraint) Host builds keep hindsight.service running and invoke authorized memory-intensive Gradle work only through /home/hermes/.local/bin/codex-memory-run, one heavy command and one worker at a time, with no unscoped fallback.
- (decision) Before lifecycle edits, read the full DAT changelog from baseline 0.4.0 through target 0.9.0 and map every breaking or behaviorally relevant change to a code decision in docs/dat-migration.md.
- (decision) The event reducer and its tests are bound to the canonical transition graph from architecture-diagrams.md, including pause and resume, failure and retry, disconnect from every active state, and cleanup back to NO_DEVICE.
- (decision) WearableSnapshot, telemetry, transition history, and structured session records collectively own every field required by wearable-state-and-diagnostics.md; UI adds no parallel state.
- (decision) Physical acceptance evidence targets Meta Adventurer Standard 49 mm and records setup metadata plus per-case transitions, first-frame result, latency, recovery, and layer error exactly as acceptance.md requires.
- (constraint) Delivery preserves upstream traceability and uses the canonical main to baseline to feat/dat-0.9 to feat/adventurer sequence, baseline and final commit intents, and tag gates.
- (question) Resolve GitHub fork owner before publishing remotes or pushing the fork.
- (question) Resolve phone model, Android version, Meta Wearables setup, and DAT registration before physical acceptance.
- (decision) A first frame means a callback carrying usable image data; DAT 0.9 codec-configuration-only packets do not satisfy readiness.
- (event) Input reconciliation complete; all blocking gaps scheduled for spine correction.
- (decision) Resolve ownership: WearableRuntime coordinates commands, reducer, recovery, and telemetry; MetaDatWearableAdapter exclusively owns DAT handles and SDK collectors.
- (decision) Preserve the VisionClaw preview seam with uncompressed app-owned I420 frame envelopes for this milestone; set DAT compression false and keep bitmap conversion off main.
- (decision) Background and phone lock do not clear desiredStreaming or tear down the session; foreground service keeps the process live and Activity recreation only reattaches. Process death resets intent and never restarts the camera automatically.
- (decision) Fence lifecycle generations: every command, SDK event, transition, and frame carries a generation ID; reducer discards retired generations.
- (decision) Retain a reducer-owned bounded history of the latest 256 transitions per active session in addition to live transition delivery.
- (decision) Opt out of DAT analytics and crash reporting by default through manifest metadata; any future opt-in requires explicit informed approval.
- (decision) Treat DAT 0.9.0 on the VisionClaw baseline toolchain as the target candidate until a minimal compatibility assemble proves the combination; only then pin the combined stack.
- (assumption) Extend the canonical graph with explicit user-stop edges back to DEVICE_CONNECTED and startup-failure edges to STREAM_FAILED; adopt the same edges into the SPEC companion before implementation readiness.
- (event) Reviewer Gate findings triaged; clear fixes selected and four user-review assumptions remain.
- (decision) Recovery targets the failed lifecycle layer: session failures recreate from SESSION_STARTING, while camera or stream failures restart from CAMERA_STARTING only when the session remains usable.
- (decision) DAT teardown binds postconditions rather than unverified call spelling: camera/stream terminated, capability detached from session, collectors cancelled, then session stopped; docs/dat-migration.md records the exact verified 0.9 calls.
- (decision) Brownfield migration may extract DAT-bound lifecycle coordination from StreamViewModel into the provider-neutral runtime and Meta adapter while preserving surrounding preview and integration behavior.
- (event) Gate recheck high findings corrected in the decision record.
- (decision by user) User adopted AD-2: process-scoped runtime coordinates lifecycle while MetaDatWearableAdapter exclusively owns DAT handles.
- (decision by user) User adopted AD-3: serialized reducer with generation fencing and bounded transition history.
- (decision by user) User adopted AD-5: uncompressed app-owned I420 frames at MEDIUM 24 FPS for the milestone preview seam.
- (decision by user) User adopted AD-7: foreground background-lock continuity, state-driven recovery, and no automatic camera restart after process death.
- (decision by user) User adopted the explicit user-stop and startup-failure transition edges for both the spine and canonical architecture diagram companion.
- (event) spine finalized
