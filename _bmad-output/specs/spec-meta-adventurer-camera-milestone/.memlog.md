---
topic: Meta Adventurer camera milestone for VisionClaw Android
updated: 2026-08-27T23:21
---

- (direction) Run in headless express mode because the supplied project plan is rich enough to distill without elicitation.
- (decision) Scope is only the first hardware milestone: VisionClaw Android plus latest stable Meta DAT 0.9.x plus explicit Meta Adventurer support plus a stable real camera stream.
- (capability) CAP-1: Establish a reproducible unmodified Android baseline; success is assembleDebug completing and docs/baseline.md recording source commit, DAT, AGP, Kotlin, SDK levels, warnings, problems, and relevant files.
- (capability) CAP-2: Migrate the existing Android sample deliberately to the latest stable DAT 0.9.x; success is a compiling app whose documented migration accounts for every applicable changelog change.
- (capability) CAP-3: Accept and identify DeviceType.META_GLASSES without assuming Ray-Ban Meta behavior; success is explicit Adventurer handling and diagnostic logging without rejecting the device for not being RAYBAN_META.
- (capability) CAP-4: Drive camera readiness through an explicit wearable state machine; success is that readiness occurs only after the first real frame and teardown/reconnect transitions are observable.
- (capability) CAP-5: Provide an in-app developer diagnostics surface; success is that device, DAT, session, camera, stream, frame, health, route, provider placeholder, gateway placeholder, last tool placeholder, and last error status are visible without Logcat.
- (capability) CAP-6: Provide a live preview and camera telemetry; success is visible Adventurer frames plus measured startup latency, first-frame latency, reconnect latency, incoming FPS, received frames, and frames forwarded.
- (capability) CAP-7: Recover the hardware vision pipeline across the defined lifecycle test matrix; success is clean recovery after glasses disconnect/reconnect and power cycling, app background/foreground, phone lock/unlock, network changes, Meta AI state changes, and repeated camera start/stop.
- (capability) CAP-8: Produce exact physical-device test instructions and evidence boundaries; success is a tester can validate on Meta Adventurer Standard 49 mm and the implementation does not claim hardware support before that test passes.
- (constraint) Start from https://github.com/Intent-Lab/VisionClaw and use samples/CameraAccessAndroid as the Android application base; do not rewrite from scratch.
- (constraint) Preserve useful existing Meta DAT access, camera streaming, phone-camera fallback, visual throttling, audio handling, Gemini Live, OpenClaw routing, reconnect, WebRTC, settings, and session-state code where compatible.
- (constraint) Inspect repository HEAD and build it unchanged before any DAT upgrade; baseline fixes may address build blockers only.
- (constraint) Read the complete official Meta DAT changelog from the repository's current version through the selected stable 0.9.x version before changing lifecycle code.
- (constraint) Use the consolidated DAT lifecycle DeviceSession.addCamera to Camera.stream and teardown with Camera.stop plus DeviceSession.removeCamera wherever required by the selected release.
- (constraint) The hardware state model includes NO_DEVICE, DEVICE_CONNECTED, SESSION_STARTING, SESSION_READY, CAMERA_STARTING, STREAM_WAITING, FIRST_FRAME_RECEIVED, STREAM_ACTIVE, STREAM_PAUSED, STREAM_FAILED, and DISCONNECTED.
- (constraint) A started stream is not a usable vision pipeline until an actual first frame has arrived.
- (constraint) Log device identifier, name, type, capabilities, battery, thermal state, DAT registration, device session, and camera lifecycle without secrets or raw private audio.
- (constraint) Classify errors by the responsible layer; in this milestone use DEVICE, DAT, CAMERA, NETWORK, and AUTH where applicable rather than a generic AI failure.
- (constraint) Structured session logs include session_id, device_type, DAT state, camera state, first-frame latency, frames received, frames forwarded, disconnect reason, and layer-specific errors.
- (constraint) The diagnostics screen must expose Active device, Device type, Device ID, DAT registration, Device session, Camera, Stream, First frame, Last frame timestamp, Incoming FPS, Total received frames, Frames sent to AI, Bluetooth input/output route, Battery, Thermal, Realtime provider/connection, Hermes gateway, Last tool call, and Last error; out-of-scope integrations remain explicit NOT_CONFIGURED placeholders.
- (constraint) Keep WearableAdapter independent from future RealtimeProvider, Operator, and Tools layers; this milestone may change only the Meta Wearables DAT adapter and the Android diagnostics around it.
- (constraint) Do not perform experimental work directly on a known-good branch; preserve baseline and milestone tags before later work.
- (constraint) Use the repository branch sequence relevant to this milestone: baseline, feat/dat-0.9, and feat/adventurer, based on the fork's main branch.
- (constraint) Create baseline-visionclaw only after a known-good baseline, and adventurer-camera-working only after physical Adventurer acceptance; never tag unverified hardware support.
- (constraint) Use commit intent chore: establish working Android baseline for baseline work and feat: support Meta Adventurer with DAT 0.9 for the completed hardware milestone.
- (constraint) On this 8 GiB no-swap host, keep hindsight.service running and run any authorized heavy Gradle command through /home/hermes/.local/bin/codex-memory-run with one memory-intensive command at a time.
- (constraint) Do not run a monolithic TypeScript typecheck or any full Paperclip server typecheck; they are unrelated and unsafe on this host.
- (non-goal) Do not implement or configure Adventurer audio loopback or production Bluetooth audio routing in this milestone.
- (non-goal) Do not implement RealtimeProvider abstraction, Gemini regression work, OpenAI Realtime, image forwarding to OpenAI, gateway, Hermes, ChatGPT OAuth, Home Assistant, durable memory, coding delegation, or conversational modes.
- (non-goal) Do not implement continuous life recording, facial recognition, always-on cloud camera upload, iOS, store release, custom Meta firmware, Meta AI replacement, or a complex visual UI.
- (non-goal) Do not optimize or benchmark full product battery behavior yet beyond avoiding accidental continuous cloud upload.
- (assumption) The latest stable DAT release satisfying the requested 0.9.x range will be resolved from official Meta sources at implementation time and pinned explicitly in docs/dat-migration.md.
- (assumption) The existing VisionClaw upstream structure still contains samples/CameraAccessAndroid; implementation must stop and report if upstream has materially changed.
- (assumption) Future-layer fields on the diagnostics screen can be rendered as NOT_CONFIGURED without implementing those layers.
- (question) Which GitHub user or organization should own the required VisionClaw fork and remotes? Local work may begin from upstream, but creating or pushing the fork requires that destination and authorization.
- (question) Which Android phone model, Android version, and Meta Wearables app/DAT registration setup will be used for the physical acceptance run?
- (decision) docs/baseline.md and docs/dat-migration.md are required repository artifacts; migration documentation lists breaking changes and the code decisions made for each applicable change.
- (decision) Manual acceptance must record Adventurer detection as META_GLASSES, camera start, arrival of real frames, and clean recovery after disconnect before the milestone is described as hardware-verified.
- (note) Wrapper-only content: detailed later-phase architecture and examples were intentionally not duplicated because the user explicitly limited this assignment to the first hardware milestone; their boundaries are retained as non-goals.
- (event) Initial intent distillation complete; rendering SPEC.md and companions next.
- (note) The migration record preserves that DAT 0.8 introduced DeviceType.META_GLASSES and requires verifying that the selected stable 0.9.x release retains the expected API behavior.
- (event) Coherence validation passed: eight stable unique capabilities each have intent and success; constraints bend design; non-goals are explicit; the success signal is physically demonstrable; prose is lean; assumptions and open questions expose unresolved facts.
- (event) Preservation validation passed: every load-bearing claim for the explicitly scoped first hardware milestone is represented in SPEC.md or a named companion; later product phases are retained as explicit non-goals and wrapper-only detail is recorded rather than silently dropped.
- (event) Spec finalized for the Meta Adventurer camera milestone.
- (decision by user) The canonical project name and repository name are both lowercase maria; all downstream artifacts, local paths, remotes, and repository setup should use maria unless an external platform imposes a different display format.
- (event) Coherence revalidation passed after naming update: capability IDs and capability content are unchanged, and the lowercase maria repository-name constraint introduces no contradiction.
- (event) Preservation revalidation passed: the user-supplied project and repository name maria is explicit in the kernel and delivery companion.
- (event) Spec update finalized with canonical project and repository name maria.
- (decision by user) User adopted explicit startup-failure and user-stop edges in the canonical hardware state graph during architecture finalization.
- (event by user) architecture-diagrams.md synchronized with the adopted architecture spine transition graph.
- (decision) User selected a personal GitHub repository named maria, owned by the personal account authenticated when the fork is created; preserve upstream VisionClaw traceability.
- (decision) Physical acceptance will use a Samsung Galaxy Z Fold7 running Android 16 with Meta Adventurer glasses.
- (decision) Physical acceptance records Meta AI app version 286.1.0.17.162 as the initial companion-app environment.
- (question) Confirm the DAT registration result by observing Wearables.registrationState after maria starts registration through Meta AI; record whether it reaches REGISTERED and whether Developer Mode is enabled for the linked Adventurer.
- (event) Coherence revalidation passed: all eight capability IDs and intent/success pairs remain unchanged; the personal repository and fixed physical-test environment are explicit; the only unresolved operational gate is observed DAT registration and Developer Mode.
- (event) Preservation revalidation passed: the user's personal GitHub ownership decision, Galaxy Z Fold7 with Android 16, and Meta AI 286.1.0.17.162 all land in SPEC.md or named companions.
- (event) Spec update finalized with repository ownership and physical acceptance environment.
